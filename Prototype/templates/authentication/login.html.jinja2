{% extends 'base.html.jinja2' %}

{%  block title %}
    <title>Connexion</title>
{% endblock %}

{% block content %}
    <div class="container">
    <form method="POST" action="{{ url_for('auth.login') }}">
        <h1>Connexion</h1>
        {{ login_form.hidden_tag() }}
        {{ login_form.username(placeholder="Nom d'utilisateur") }}
        {{ login_form.password(placeholder='Mot de passe') }}
        <button type="submit" >Connexion</button>
    </form>
    </div>
    <div class="container">
    <h2>Vous ne possédez pas de compte ? Inscrivez-vous ici :</h2>
    <a href="{{ url_for('auth.register') }}"> Inscription</a>
    </div>
{% endblock %}
